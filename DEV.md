# 中文 AI 味检测器 · DEV.md

> 项目开发圣经。写代码对着这份文档来，不从记忆来。名字：SlopLint —— 中文 AI 味检测器（仓库 sloplint）。

---

## 1. 定位（一句话）

检测中文写作里的「AI 味」，告诉作者"哪几句、为什么像 AI 写的"。不替人改写，只帮人判断。

## 2. 差异化（为什么做、凭什么赢）

| 维度 | 我们 | stop-slop-zh | humanizer |
|---|---|---|---|
| 形态 | 网页 + skill 双形态 | 仅 skill | 仅 skill |
| 功能 | 检测 + 点评 | 改写 | 改写 |
| 受众 | 自媒体写作者 + agent 用户 | 开发者 | 开发者 |
| 引擎 | 纯规则（网页）+ LLM（skill） | 纯 LLM | 纯 LLM |
| 语言 | 中文专属 | 中文 | 英文 |

核心差异化就一句话：**stop-slop-zh 给"不痛的人"做了个"要门槛的改写工具"，我们给"最痛的人"做个"零门槛的检测工具"，同时不放弃 agent 用户。**

## 3. 双形态架构

- **网页版**：纯前端，面向不懂代码的公众号写作者/小红书博主。粘贴 → 本地正则扫描 → 评分 + 标红。零门槛、链接可分享。
- **skill 版**：一个 SKILL.md，面向有 agent 的用户（Claude Code / Cursor / Hermes）。把 skill 装进去，说"检测这段文字的 AI 味"，agent 按规则库检测并点评。

两者的差异在**执行引擎**，不在规则：
- 网页版 = 正则/词表，只抓"硬" AI 味（命中词表、句式套路），毫秒级、免费、无依赖。
- skill 版 = LLM 读规则，额外能抓"软" AI 味（逻辑、语气、节奏、总分总八股），更准，天然能顺带改写。

规则内容（12 个模式 + 词表）两版完全共享，**单一数据源**。

## 4. 核心资产：规则库（单一数据源）

规则库是一份 JSON，12 个模式。网页版 `import` 它；skill 版用脚本从它生成 SKILL.md。

每个模式的结构：
```json
{
  "id": "empty-colon",
  "name": "冒号空转句",
  "weight": 3,
  "evidence": "corpus",
  "R": "9.4",
  "type": "regex",
  "keywords": ["以下几个方面", "主要包括"],
  "regex": "(以下几个方面|主要包括)[^：:]{0,8}[：:]",
  "comment": "点评模板"
}
```

`type` 字段四种：`regex`（默认，词表+正则）、`line-start`（行首匹配）、`density`（密度超阈值）、`density-below`（密度低于阈值）。

**规则库清单（按证据强度分层）：**

数据验证特征（来自 lieflat-less-ai-tone 283万字对照研究，weight 3）：
1. 冒号空转句（R=9.4）——「主要有以下几种场景：」空转冒号
2. 提示语冒号（R=3.8）——「核心是：」「说白了：」
3. 对举结构（R=3.4）——「不是 A 而是 B」
4. 起首语（R=3.2）——「说白了」「说穿了」
5. 序数词小标题（R=3.1）——「一、」「1.」开头
6. 破折号过密（R=3.0）——密度 >1.5/千字
7. 翻译腔（R=2.6-5.3）——「当…时」「对于…来说」「这意味着」句首「然而」

数据验证较弱（weight 2）：
8. 顿号过密（R=1.8）——密度 >2.5/千字
9. 缺数据支撑（R=0.35，反向）——数字密度 <8/千字

语义层（仅 skill 版，LLM 判断）：
10. 段首零回指评论（R=4.4，区分力最强）
11. 拟人化喻体（R=7.3）
12. 相邻句结构同构（R=2.0）

语感特征（weight 1，未经语料检验，待验证）：
13. 万能开场 / 总结腔 / 无立场平衡 / 成语填充 / 形容词通胀

> 注：2026-09 重构。删除被证伪的「句子长度均匀（R=0.87）、翻译腔动词、抽象主语、名词化堆叠、值得注意的是」；新增数据验证特征。依据 lieflat-less-ai-tone 研究。

## 5. 评分逻辑（决策 1 = B 加权）

- 权重分三档：数据验证显著 3 / 数据验证较弱或反向 2 / 语感待验证 1
- 加权命中分 = Σ(模式权重 × 命中次数)
- 浓度 = 100 × [加权命中分 / (加权命中分 + 校准常数)]
- 输出 0–100 整数，配「低 / 中 / 高」色阶（绿/黄/红）
- 校准常数 = 6.0，用真实样本迭代校准

## 6. 输出形式（决策 2 = A+B 结合）

1. 顶部：大号浓度数字 + 色阶条
2. 中间：原文高亮命中处（悬停显示"这是 XX 模式"）
3. 底部：命中清单（第几句 / 哪个模式 / 触发词 / 点评）

## 7. 数据流

**网页版：**
```
粘贴文字 → 分句 → 逐模式跑正则+词表 → 统计命中 → 加权评分 → 标红 + 命中清单
```

**skill 版：**
```
用户给文字 → agent 读 SKILL.md 规则 → 检测 12 模式 → 评分 + 逐句点评（+ 可选改写）
```

## 8. 项目结构

```
sloplint/
├── DEV.md                    # 本文档
├── rules/
│   └── patterns.json         # 12 模式（单一数据源）
├── web/                      # 网页版（Vite + 原生 JS）
│   ├── index.html
│   ├── src/
│   │   ├── main.js           # 入口
│   │   ├── scanner.js        # 扫描引擎（读 patterns.json）
│   │   └── patterns.json     # 或 import 自 ../rules
│   └── package.json
├── skill/
│   ├── SKILL.md              # skill 版（给 agent）
│   └── build_skill.mjs        # 从 rules/patterns.json 生成 SKILL.md
└── README.md                 # 开源说明 + 公众号引流
```

## 9. 技术栈（决策 = Vite + 原生 JS）

- 网页：Vite + 原生 JavaScript（不用 React/Next.js，检测是纯本地规则，不需要状态管理）
- skill：纯 Markdown（SKILL.md），遵循 Agent Skills 格式
- 部署：Vercel（网页），GitHub 开源（skill + 规则库）
- 无后端、无数据库、无 LLM API（第一版）

## 10. MVP 范围

**做：**
- 规则库 JSON（12 模式完整定义）
- 网页版：文本框、扫描引擎、加权评分、原文高亮、命中清单、点评
- skill 版：SKILL.md + 生成脚本
- README（开源说明）

**不做（这版）：**
- LLM 改写（skill 版里作为"可选"提一句，不实现）
- 登录 / 用户系统 / 历史记录 / 后端 / 数据库
- 移动端 App

## 11. 开发步骤（顺序）

1. 规则库：写 `rules/patterns.json`，12 模式完整词表 + 正则 + 权重 + 点评
2. 网页版骨架：Vite 初始化，文本框 + 扫描引擎跑通一个模式
3. 网页版全量：12 模式全接上，评分 + 标红 + 清单
4. skill 版：`build_skill.mjs` 从 JSON 生成 SKILL.md
5. 校准：拿真实文章跑，调权重和校准常数
6. README + 部署 Vercel + GitHub 开源

## 12. 验收标准

- [x] 明显 AI 味文字，浓度 > 70，标红准确 —— 通过（AI 测试文 81/100）
- [x] 真人文字，浓度 < 30 —— 通过（真人文 0/100）
- [x] 网页毫秒级出结果，无网络请求 —— 通过
- [ ] skill 版在 Hermes / Claude Code 里能跑通检测 —— 待验证

### 真实文章校准记录（2026-09）

三级梯度测试，区分力符合预期：

| 测试样本 | 结果 | 说明 |
|---|---|---|
| GPT 纯生成文 | 高 | 无风格约束的默认 AI 输出 |
| 用公众号 prompt 生成文 | 中 | 提示词降低了 AI 味，但没消除 |
| 知名自媒体真人文章 | 低 | 零误伤 |

关键洞察：「中」这一档是 SlopLint 的独特价值所在——提示词/改写工具不会告诉你「还剩多少 AI 味」，检测器能。改写是吃药，检测是体检。
